{
  "name": "queue-sqs",
  "version": "1.0.0",
  "description": "A wrapper for sqs that complies with the dreamup queue interface",
  "main": "src/index.js",
  "scripts": {
    "test": "npm run build && mocha --require test/env.js",
    "build": "tsc"
  },
  "repository": {
    "type": "git",
    "url": "github.com/dreamup-ai/queue-sqs"
  },
  "author": "Shawn Rushefsky",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.310.0",
    "interfaces": "github:dreamup-ai/interfaces"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0"
  }
}
